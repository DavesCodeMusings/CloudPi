---
- name: Update System to Latest Packages
  hosts: localhost
  connection: local
  become: true

  tasks:
  - name: Updating Apt cache
    apt:
      update_cache: yes

  - name: Updating all packages
    apt:
      upgrade: full

  - name: Determining if reboot is required
    stat:
      path: /var/run/reboot-required
    notify:
    - Reboot required

  handlers:
  - name: Reboot required
    debug:
      msg: A reboot is required.
